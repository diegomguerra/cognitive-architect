VYR 8.1(391) — Design System v1 + biomarcadores mais honestos

Novidades visuais
- Tela Insights nova, no estilo Oura (Design System v1). Cards de biomarcadores reorganizados.

Correções de medições
- FC Repouso (RHR): quando o anel só envia HR cru, derivamos o RHR a partir do percentil 10 do HR (estimativa mais confiável que mostrar "—").
- HR Repouso: snapshot dos pacotes antes do flush, para que o pipeline de derivação receba os dados corretos.
- SpO2: JStyle cmd 0x66 (Automatic) corrigido + Colmi com multi-stream realtime (HR/SpO2/Stress paralelos).
- Estresse: mesma lógica de fallback do RHR — exibe última leitura ou média 7d, nunca "—" se há dado nas últimas 7d.

Sob o capô
- CI/fastlane: sigh + update_code_signing_settings auto-fetch (uploads mais estáveis).
