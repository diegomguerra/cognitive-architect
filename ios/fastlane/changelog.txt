VYR 8.1(409) — 5 bugs fix (Sono dedup + per-vendor + Colmi flush)

O que mudou
- Sono: dedup minuto-a-minuto restaurado (regrediu no rewrite 407). Antes mostrava
  38.4h em 1 noite — agora cada minuto da noite recebe um único stage (timeline),
  total honesto (~7-11h). REM/Leve/Profundo voltam ao detalhe do card.
- Card "Fonte do dado" agora reflete o anel REALMENTE conectado: "JStyle X5"
  pro Diego, "Colmi R09" pra Lídia/Daniele. Era hardcoded JStyle pra todos.
- Plugin Colmi: força flush de history buffers (steps, spo2, sleep) ao fim do
  syncColmi independente do header packet subIdx==0. Anel R09 frequentemente
  pula o header, deixando samples no buffer pra sempre. Agora qualquer dado
  acumulado é uploaded no final do sync.
- "Medir Agora" oculto pra Colmi (anel R09 não suporta cmd 0x28 manual,
  protocolo diferente). Mostra label "auto · Colmi" no lugar. Era o erro
  "JStyle vendor: colmi" que aparecia pra Lídia/Daniele.
- Diagnóstico Card Energia/Estabilidade: zero é correto quando anel está
  offline ou sample count baixo (Daniele: ring_samples_total=2 hoje).
  Engine score retorna 0 honesto quando data_confidence é "insufficient".

Backend (sem mudanças nesta build)
- parse-vendor-raw v18 ACTIVE (firmware extractor + SDF HRV) inalterado.

Próximos passos pra build 410
- Implementar Big Data V2 (ID 39) pra Colmi Sleep history (cmd 0x44 retorna
  erro no R09 — firmware exige protocolo V2).
- Decode PPG multi-canal real (libBleSDK.a disassembly).
- Colmi reconnect robustness (CONNECT_TIMEOUT retry).
